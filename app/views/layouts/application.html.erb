<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">

    <!-- Primary Meta Tags -->
    <title>NekoDNS — Chatops for your DNS records</title>
    <meta name="title" content="NekoDNS — Chatops for your DNS records">
    <meta name="description" content="NekoDNS integrates with your DNS provider to let you manage your domains on-the-go through your favourite instant messaging platform.">

    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://nekodns-staging.herokuapp.com/">
    <meta property="og:title" content="NekoDNS — Chatops for your DNS records">
    <meta property="og:description" content="NekoDNS integrates with your DNS provider to let you manage your domains on-the-go through your favourite instant messaging platform.">
    <meta property="og:image" content="https://nekodns-staging.herokuapp.com/NekoDNS.png">

    <!-- Twitter -->
    <meta property="twitter:card" content="summary_large_image">
    <meta property="twitter:url" content="https://nekodns-staging.herokuapp.com/">
    <meta property="twitter:title" content="NekoDNS — Chatops for your DNS records">
    <meta property="twitter:description" content="NekoDNS integrates with your DNS provider to let you manage your domains on-the-go through your favourite instant messaging platform.">
    <meta property="twitter:image" content="https://nekodns-staging.herokuapp.com/NekoDNS.png">

    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>

    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png">
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
    <link rel="manifest" href="/site.webmanifest">

    <% if Rails.env.production? %>
      <%= render 'ga' %>
    <% end %>
  </head>

  <body>
  <nav class="navbar navbar-expand navbar-light bg-light">
    <div class="container">
      <a href="<%= root_path %>" class="navbar-brand">NekoDNS
        <small><span class="badge badge-secondary align-text-top">ALPHA</span></small>
      </a>

      <ul class="navbar-nav mr-auto">
        <% if current_user %>
          <%= render 'navbar' %>
        <% end %>
      </ul>

      <% if current_user %>
        <%= render 'logged_in_indicator' %>
      <% else %>
        <a href="<%= login_path %>" class="btn btn-outline-dark">Login</a>
      <% end %>
    </div>
  </nav>
  <main class="container my-3">
    <%= render 'flash_messages' %>
    <%= yield %>
  </main>
  </body>
</html>
